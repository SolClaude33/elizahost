# CRITICO - Necesario para Railway
DAEMON_PROCESS=true
TRUST_PROXY=true

# ============================================
# CONFIGURACIÓN DE LLM (OpenAI o Grok/XAI)
# ============================================

# OPCIÓN 1: OpenAI Real (clave debe empezar con 'sk-')
OPENAI_API_KEY=sk-tu_clave_api_openai
OPENAI_API_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o

# OPCIÓN 2: Grok/XAI - Variables específicas (RECOMENDADO para Grok, clave debe empezar con 'xai-')
# XAI_API_KEY=xai-tu_clave_api_grok
# XAI_MODEL=grok-3-latest

# OPCIÓN 3: Grok/XAI - Variables genéricas (compatibilidad)
# OPENAI_API_KEY=xai-tu_clave_api_grok
# OPENAI_API_BASE_URL=https://api.x.ai/v1
# OPENAI_MODEL=grok-beta

# ============================================
# BNB Chain / EVM (Niya)
# ============================================
# RPC de BNB Chain (Mainnet). Puedes usar tu provider preferido.
BNB_RPC_URL=https://bsc-dataseed.binance.org

# Wallet EVM (0x...)
BNB_WALLET_ADDRESS=0xTuWalletBNB

# Private key EVM (hex, con o sin 0x). NO la compartas.
BNB_PRIVATE_KEY=0xTuPrivateKeyBNB

# Datos de mercado (opcional, usado por el agente para consultas de precio/pares si lo implementas)
DEXSCREENER_API_KEY=tu_dexscreener_api_key

# Solana/Blockchain
# IMPORTANTE: SOLANA_PRIVATE_KEY debe estar en formato base58 (exportado de Phantom/Solflare)
SOLANA_RPC_URL=https://mainnet.helius-rpc.com/?api-key=TU_HELIUS_API_KEY
SOLANA_PUBLIC_KEY=tu_wallet_publica
SOLANA_PRIVATE_KEY=tu_wallet_privada_base58
HELIUS_API_KEY=tu_helius_api_key

# Plugin Solana - Variables adicionales requeridas
# SOL_ADDRESS: Dirección del token nativo SOL (Wrapped SOL) - VALOR POR DEFECTO
SOL_ADDRESS=So11111111111111111111111111111111111111112

# SLIPPAGE: Porcentaje máximo de slippage para swaps (en basis points)
# 50 = 0.5%, 100 = 1%, 500 = 5%
# Recomendado: 100 (1%) para swaps normales
SLIPPAGE=100

# BIRDEYE_API_KEY: API key de Birdeye para datos de mercado (opcional pero recomendado)
# Obtener en: https://birdeye.so/
# Nota: Si no configuras esto, el plugin puede tener funcionalidad limitada
BIRDEYE_API_KEY=tu_birdeye_api_key

# Twitter/X Integration (OAuth Tokens - Metodo Seguro)
# IMPORTANTE: Los nombres correctos de variables son TWITTER_*, no X_*
# IMPORTANTE: Nunca uses username/password directamente. Usa OAuth tokens exclusivamente.
TWITTER_API_KEY=tu_api_key
TWITTER_API_SECRET_KEY=tu_api_secret
TWITTER_ACCESS_TOKEN=tu_access_token
TWITTER_ACCESS_TOKEN_SECRET=tu_access_secret
TWITTER_BEARER_TOKEN=tu_bearer_token

# Puerto para WebSocket (Railway lo asigna automaticamente)
PORT=3000

# Habilitar UI web de ElizaOS (opcional)
ELIZA_UI_ENABLE=true

# URL pública del agente (generada por Railway)
ELIZA_PUBLIC_URL=https://elizahost-production.up.railway.app

# Endpoint para AMICA - CORS (permite conexiones desde otros dominios)
CORS_ORIGIN=*
# O especifica dominios concretos:
# CORS_ORIGIN=https://amica.app,https://replit.app,https://tu-dominio.com

# Seguridad (opcional pero recomendado)
SECRET_SALT=una_cadena_aleatoria_larga_y_segura

# Autenticación API (opcional pero recomendado para producción)
ELIZA_SERVER_AUTH_TOKEN=tu_token_de_autenticacion_opcional

# ============================================
# PLUGIN ELEVENLABS (Voz - Opcional)
# ============================================
# Solo necesario si usas el plugin @elizaos/plugin-elevenlabs
# Obtener API key en: https://elevenlabs.io/
# Nota: Tu personaje tiene configurado voice.model: "Yuki", pero el plugin elevenlabs
# no está en la lista de plugins, así que probablemente no necesites esto
# ELEVENLABS_API_KEY=tu_elevenlabs_api_key

# ============================================
# PERSONAJE (Opcional)
# ============================================
# Para cambiar entre personajes (amica-agent o niya-agent)
# ELIZA_CHARACTER_NAME=niya-agent
